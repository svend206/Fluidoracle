# =============================================================
# Hydraulic Filter Expert Platform — Environment Configuration
# =============================================================
# Copy this file to .env and fill in your keys:
#   cp .env.example .env
#
# Your .env file should NEVER be committed to git.
# It's already listed in .gitignore.
# =============================================================

# --- API Keys ---

# Anthropic API key (for answer generation via Claude)
# Get one at: https://console.anthropic.com
ANTHROPIC_API_KEY=

# OpenAI API key (for embeddings in the RAG pipeline)
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --- Model Configuration ---

# Claude model for answer generation
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Embedding model for RAG pipeline
EMBEDDING_MODEL=text-embedding-3-small

# --- Paths ---

# ChromaDB storage (relative to project root)
CHROMA_PATH=./vector-store

# SQLite database for community data
DATABASE_PATH=./data/community.db

# --- Invention Sessions ---

# Passphrase for private invention/brainstorming sessions
# Only you should know this. Leave blank to disable the feature.
INVENT_PASSPHRASE=

# --- Admin ---

# Secret key for admin endpoints (question cleanup, etc.)
# Generate one: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_KEY=

# --- Site Gate ---

# Password for the site-wide gate (private beta).
# Leave blank to disable the gate entirely.
VITE_SITE_PASSWORD=spray

# --- CORS ---

# Frontend dev server origin (comma-separated for multiple)
CORS_ORIGINS=http://localhost:5173
